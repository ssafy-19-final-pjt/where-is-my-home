package com.ssafy.home.domain.wish.repository;

import com.ssafy.home.domain.wish.entity.WishDeal;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WishDealRepository extends JpaRepository<WishDeal, Long> {
}
